# Memory / Skill Recipe

This recipe directory is reserved for formal experiments that compare Direct, ReAct, Memory, Skill, and Memory+Skill agent baselines under the same environment, model, and RL algorithm settings.

Keep prompt-based experiments in `examples/memory_skill_agent/` until the configuration is stable, then move reproducible experiment scripts here.
