# 目录架构说明

本文档描述当前仓库的目录结构和职责边界。

项目定位：基于 [verl-agent](https://github.com/langfengQ/verl-agent) 精简的科研 Agent 基础设施，**保留 agent 系统和 benchmark 交互环境**，用于快速搭建 agent baseline 和在 ALFWorld、WebShop 等环境中进行实验。

---

## 顶层目录

```text
agent_infra/
├── agent_system/         # Agent 系统：baseline、环境封装、记忆、技能、rollout
├── run/                  # 独立运行入口：配置驱动的 Agent-环境 交互脚本
├── .github/              # GitHub 协作配置
├── .vscode/              # VS Code 本地开发配置
├── ENV_SETUP.md          # 环境激活说明
├── README.md             # 项目说明
├── .gitignore            # Git 忽略规则
└── 目录架构说明.md        # 本文件
```

---

## Agent 系统目录

### `agent_system/`

LLM Agent 交互系统核心目录，提供 agent baseline、环境封装、多轮交互、记忆、技能、状态跟踪、动作解析和 prompt 构造等能力。

```text
agent_system/
├── agents/               # Agent baseline 实现
├── environments/          # 环境抽象与封装
├── memory/               # 记忆模块
├── skills/               # Skill bank 与检索
├── state/                # 显式状态跟踪
├── action/               # 动作解析与校验
├── prompt_builder/       # 组合式 prompt 构造
├── evaluation/           # 行为分析与成本统计
├── multi_turn_rollout/   # 多轮 agent-environment 交互循环
└── reward_manager/       # 奖励管理
```

---

### `agent_system/agents/`

Agent baseline 抽象和实现。

| 文件 | 说明 |
|------|------|
| `base.py` | `BaseAgent` 和 `AgentOutput`，统一返回格式 |
| `direct_agent.py` | Direct baseline，直接从观测生成动作 |
| `react_agent.py` | ReAct baseline，Thought/Action 通道 |
| `reflexion_agent.py` | Reflexion baseline，历史反思增强 prompt |
| `memory_agent.py` | Memory baseline，检索长期记忆构造 prompt |
| `skill_agent.py` | Skill baseline，检索可复用技能构造 prompt |

---

### `agent_system/environments/`

环境抽象与环境管理。

| 路径 | 说明 |
|------|------|
| `base.py` | 环境基类与统一接口 |
| `env_manager.py` | 环境实例管理、并行和 group 组织 |
| `prompts/` | 各环境 prompt 模板（alfworld, webshop, search, sokoban 等） |
| `env_package/` | 具体环境适配与第三方环境源码 |

---

### `agent_system/environments/env_package/`

支持 6 个 benchmark 环境：

| 环境 | 路径 | 说明 |
|------|------|------|
| ALFWorld | `alfworld/` | 具身 AI 文本交互任务 |
| WebShop | `webshop/` | 电商网页交互任务 |
| Search | `search/` | 搜索与工具调用任务 |
| Sokoban | `sokoban/` | 推箱子视觉推理任务 |
| Gym Cards | `gym_cards/` | 视觉纸牌游戏任务 |
| AppWorld | `appworld/` | 手机 App 交互任务（实验性） |

每个环境子目录通常包含：
- `envs.py`：环境包装
- `projection.py`：观测/动作格式转换
- 第三方环境源码目录

---

### `agent_system/memory/`

记忆模块，控制每步输入包含的历史信息。

| 文件 | 说明 |
|------|------|
| `base.py` | 记忆模块基础接口 |
| `memory.py` | 默认记忆实现 |
| `trajectory_memory.py` | 轨迹级短期记忆 |
| `experience_memory.py` | 成功经验长期记忆 |
| `failure_memory.py` | 失败经验长期记忆 |
| `retriever.py` | 记忆检索器（keyword baseline） |
| `storage.py` | JSONL 持久化 |

---

### `agent_system/skills/`

Skill bank 和检索。

| 文件 | 说明 |
|------|------|
| `base.py` | `Skill` 数据结构 |
| `skill_bank.py` | skill 集合加载/保存 |
| `skill_retriever.py` | skill 检索 |
| `skill_evaluator.py` | skill 收益统计 |
| `skill_updater.py` | 从轨迹更新 skill |
| `banks/` | 各任务初始 skill bank |

---

### `agent_system/state/`

显式状态跟踪。

| 文件 | 说明 |
|------|------|
| `base.py` | 状态跟踪器接口 |
| `state_updater.py` | 通用状态更新器 |
| `alfworld_state_tracker.py` | ALFWorld 状态跟踪 |
| `webshop_state_tracker.py` | WebShop 状态跟踪 |

---

### `agent_system/action/`

动作解析与校验。

| 文件 | 说明 |
|------|------|
| `action_parser.py` | 从模型输出抽取可执行动作 |
| `action_normalizer.py` | 动作格式统一 |
| `action_validator.py` | 基于候选动作检查合法性 |

---

### `agent_system/prompt_builder/`

组合式 prompt 构造。

| 文件 | 说明 |
|------|------|
| `base.py` | prompt builder 接口 |
| `react_prompt_builder.py` | ReAct prompt 构造 |
| `memory_prompt_builder.py` | 注入检索记忆的 prompt |
| `skill_prompt_builder.py` | 注入检索技能的 prompt |

---

### `agent_system/evaluation/`

行为分析。

| 文件 | 说明 |
|------|------|
| `metrics.py` | success rate、平均步数等指标 |
| `failure_analyzer.py` | 失败类型统计 |
| `memory_usage_analyzer.py` | 记忆使用统计 |
| `skill_usage_analyzer.py` | skill 使用统计 |
| `cost_tracker.py` | token 成本统计 |

---

### `agent_system/multi_turn_rollout/`

多轮 agent-environment 交互循环。

| 文件 | 说明 |
|------|------|
| `rollout_loop.py` | 多轮 rollout 主循环 |
| `utils.py` | 辅助函数 |

> ⚠️ 该模块依赖 veRL 核心库（`verl.protocol.DataProto` 等），需在有 veRL 的环境中运行。

---

### `agent_system/reward_manager/`

| 文件 | 说明 |
|------|------|
| `episode.py` | 按 episode 组织 reward、done、轨迹 |

> ⚠️ 该模块依赖 veRL 核心库，需在有 veRL 的环境中运行。

---

## 独立运行目录

### `run/`

不依赖 veRL 的独立运行入口，通过 **YAML 配置文件** 组装不同的 Agent + Environment + LLM，实现 `agent × env` 的任意组合运行。

```text
run/
├── __init__.py
├── config.py             # 配置 dataclass、YAML 加载、OmegaConf 适配
├── llm_client.py         # LLM API 客户端（OpenAI / Anthropic）
├── runner.py             # 核心交互循环：Agent ↔ LLM ↔ Environment
├── run.py                # CLI 入口
├── requirements.txt      # 运行依赖
├── configs/              # YAML 配置文件
│   ├── alfworld_react.yaml
│   └── webshop_react.yaml
└── outputs/              # 运行结果（JSON）
```

**设计思路**：每份 YAML 配置指定 `agent_type` + `env.name` + `llm`，Runner 动态组装对应组件，执行交互循环。

```bash
# 不同 agent × 不同 env，只需切换配置文件
python -m run.run --config run/configs/alfworld_react.yaml
python -m run.run --config run/configs/webshop_react.yaml
python -m run.run --config run/configs/alfworld_reflexion.yaml
```

**支持的组合**：

| 维度 | 可选值 |
|------|--------|
| Agent | `react`, `reflexion`, `memory`, `skill`, `direct` |
| Environment | `alfworld`, `webshop`, `search`, `sokoban`, `gym_cards`, `appworld` |
| LLM | `openai` (兼容), `anthropic` |

> 📦 运行前需安装依赖：`pip install -r run/requirements.txt`

---

## 配置目录

### `.github/`

GitHub 仓库维护目录，包含 PR 模板、CODEOWNERS 和 CI 工作流。大部分训练相关的 workflow 已禁用或移除。

### `.vscode/`

VS Code 编辑器配置。

---

## 根目录文件

| 文件 | 说明 |
|------|------|
| `README.md` | 项目说明、安装、功能、结果 |
| `ENV_SETUP.md` | ALFWorld / WebShop 环境激活方式 |
| `.gitignore` | Git 忽略规则 |
| `目录架构说明.md` | 本文件 |

---

## 环境说明

ALFWorld 和 WebShop 已安装在独立 conda 环境中：

| 环境名 | Python | 用途 |
|--------|--------|------|
| `verl-agent-alfworld` | 3.12 | ALFWorld 具身 AI 任务 |
| `verl-agent-webshop` | 3.10 | WebShop 电商交互任务 |

激活方式详见 [ENV_SETUP.md](ENV_SETUP.md)。

其他环境（Search, Sokoban, Gym Cards, AppWorld）的环境代码保留在 `agent_system/environments/env_package/` 中，按需安装依赖后可使用。
